<Window x:Class="BytService.App.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Вход в систему"
        Height="320"
        Width="420"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Margin="0,0,0,16">
            <TextBlock FontSize="24" FontWeight="Bold" Text="БытСервис" />
            <TextBlock Foreground="#6B7280" Text="Учет заявок на ремонт бытовой техники" />
        </StackPanel>

        <TextBlock Grid.Row="1" Text="Логин" />
        <TextBox x:Name="LoginTextBox" Grid.Row="1" Margin="0,24,0,8" />

        <TextBlock Grid.Row="2" Text="Пароль" />
        <PasswordBox x:Name="PasswordBox" Grid.Row="2" Margin="0,24,0,8" />

        <TextBlock x:Name="ErrorTextBlock"
                   Grid.Row="3"
                   Margin="0,8,0,8"
                   Foreground="#B91C1C"
                   Visibility="Collapsed"
                   TextWrapping="Wrap" />

        <Button Grid.Row="4"
                HorizontalAlignment="Right"
                Width="120"
                Height="36"
                Click="SignInButton_OnClick"
                Content="Войти" />
    </Grid>
</Window>

