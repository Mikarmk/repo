<Window x:Class="BytService.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="БытСервис - рабочее место"
        Height="760"
        Width="1280"
        WindowStartupLocation="CenterScreen">
    <DockPanel Margin="16">
        <Border DockPanel.Dock="Top"
                Margin="0,0,0,16"
                Padding="16"
                CornerRadius="14"
                Background="#FF1D4ED8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock x:Name="WelcomeTextBlock"
                               FontSize="24"
                               FontWeight="Bold"
                               Foreground="White" />
                    <TextBlock x:Name="RoleTextBlock"
                               Margin="0,6,0,0"
                               Foreground="#DBEAFE" />
                </StackPanel>
                <Button Grid.Column="1"
                        Width="120"
                        Height="34"
                        Background="White"
                        Foreground="#1D4ED8"
                        Click="RefreshButton_OnClick"
                        Content="Обновить" />
            </Grid>
        </Border>

        <TabControl x:Name="MainTabControl">
            <TabItem Header="Заявки">
                <Grid Margin="8">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Border Padding="12" CornerRadius="12" Background="White">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="280" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBox x:Name="SearchTextBox"
                                     Height="36"
                                     VerticalContentAlignment="Center"
                                     TextChanged="SearchTextBox_OnTextChanged" />
                            <WrapPanel Grid.Column="1" HorizontalAlignment="Right">
                                <Button x:Name="AddRequestButton" Click="AddRequestButton_OnClick" Content="Новая заявка" />
                                <Button x:Name="EditRequestButton" Click="EditRequestButton_OnClick" Content="Изменить" />
                                <Button x:Name="DeleteRequestButton" Click="DeleteRequestButton_OnClick" Content="Удалить" />
                                <Button x:Name="CommentsButton" Click="CommentsButton_OnClick" Content="Комментарии" />
                                <Button x:Name="QrButton" Click="QrButton_OnClick" Content="QR отзыв" />
                            </WrapPanel>
                        </Grid>
                    </Border>

                    <Border Grid.Row="1" Margin="0,16,0,0" Padding="12" CornerRadius="12" Background="White">
                        <DataGrid x:Name="RequestsDataGrid"
                                  AutoGenerateColumns="False"
                                  CanUserAddRows="False"
                                  IsReadOnly="True"
                                  SelectionMode="Single"
                                  SelectionChanged="RequestsDataGrid_OnSelectionChanged">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="№" Binding="{Binding RequestNumber}" Width="120" />
                                <DataGridTextColumn Header="Дата" Binding="{Binding CreatedAt, StringFormat=d}" Width="100" />
                                <DataGridTextColumn Header="Техника" Binding="{Binding ApplianceType}" Width="120" />
                                <DataGridTextColumn Header="Модель" Binding="{Binding ApplianceModel}" Width="220" />
                                <DataGridTextColumn Header="Проблема" Binding="{Binding ProblemDescription}" Width="240" />
                                <DataGridTextColumn Header="Клиент" Binding="{Binding ClientName}" Width="180" />
                                <DataGridTextColumn Header="Статус" Binding="{Binding StatusName}" Width="160" />
                                <DataGridTextColumn Header="Мастер" Binding="{Binding AssignedMasterName}" Width="180" />
                                <DataGridTextColumn Header="План" Binding="{Binding PlannedCompletionDate, StringFormat=d}" Width="110" />
                                <DataGridTextColumn Header="Продлено до" Binding="{Binding ExtendedUntil, StringFormat=d}" Width="110" />
                                <DataGridTextColumn Header="Последний комментарий" Binding="{Binding LatestComment}" Width="260" />
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>
                </Grid>
            </TabItem>

            <TabItem Header="Статистика">
                <Grid Margin="8">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <UniformGrid Rows="1" Columns="3" Margin="0,0,0,16">
                        <Border Margin="6" Padding="16" CornerRadius="12" Background="White">
                            <StackPanel>
                                <TextBlock Foreground="#6B7280" Text="Выполнено заявок" />
                                <TextBlock x:Name="CompletedRequestsTextBlock" FontSize="28" FontWeight="Bold" />
                            </StackPanel>
                        </Border>
                        <Border Margin="6" Padding="16" CornerRadius="12" Background="White">
                            <StackPanel>
                                <TextBlock Foreground="#6B7280" Text="Среднее время ремонта, дней" />
                                <TextBlock x:Name="AverageRepairDaysTextBlock" FontSize="28" FontWeight="Bold" />
                            </StackPanel>
                        </Border>
                        <Border Margin="6" Padding="16" CornerRadius="12" Background="White">
                            <StackPanel>
                                <TextBlock Foreground="#6B7280" Text="Просроченные заявки" />
                                <TextBlock x:Name="OverdueRequestsTextBlock" FontSize="28" FontWeight="Bold" />
                            </StackPanel>
                        </Border>
                    </UniformGrid>

                    <Border Grid.Row="1" Padding="12" CornerRadius="12" Background="White">
                        <DataGrid x:Name="StatisticsDataGrid"
                                  AutoGenerateColumns="False"
                                  CanUserAddRows="False"
                                  IsReadOnly="True">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Тип техники" Binding="{Binding ApplianceType}" Width="*" />
                                <DataGridTextColumn Header="Количество заявок" Binding="{Binding RequestsCount}" Width="220" />
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>
                </Grid>
            </TabItem>

            <TabItem x:Name="UsersTabItem" Header="Пользователи">
                <Grid Margin="8">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <WrapPanel>
                        <Button Click="AddUserButton_OnClick" Content="Добавить пользователя" />
                    </WrapPanel>
                    <Border Grid.Row="1" Margin="0,16,0,0" Padding="12" CornerRadius="12" Background="White">
                        <DataGrid x:Name="UsersDataGrid"
                                  AutoGenerateColumns="False"
                                  CanUserAddRows="False"
                                  IsReadOnly="True">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="ФИО" Binding="{Binding FullName}" Width="*" />
                                <DataGridTextColumn Header="Телефон" Binding="{Binding Phone}" Width="170" />
                                <DataGridTextColumn Header="Логин" Binding="{Binding Login}" Width="140" />
                                <DataGridTextColumn Header="Роль" Binding="{Binding RoleName}" Width="180" />
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>
                </Grid>
            </TabItem>
        </TabControl>
    </DockPanel>
</Window>

